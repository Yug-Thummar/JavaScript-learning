// returns true if "every elements of array given true" for some function. else return false
let arr = [1,3,5,7,9];
let odd = arr.every(el => (el%2 != 0) );
console.log(odd);

// let arr = [1, 3, 5, 7, 9];
// let odd = arr.every(el => el % 2 != 0);
// console.log(odd); // true

